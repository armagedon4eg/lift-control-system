package com.relevator.elevator.sensor;

/**
 * The DoorSensor class is responsible for sensing the state of the lift's doors.
 */
public class DoorSensor {

    /**
     * Checks if it is safe to open the doors.
     *
     * @return True if it is safe to open the doors, false otherwise.
     */
    public boolean areDoorsSafeToOpen() {
        // Simulate sensor logic
        return true;
    }

    /**
     * Checks if it is safe to close the doors.
     *
     * @return True if it is safe to close the doors, false otherwise.
     */
    public boolean areDoorsSafeToClose() {
        // Simulate sensor logic
        return true;
    }
}
